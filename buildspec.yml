version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install
      - npm install -g @vue/cli
  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
    - 'appspec.yml'

